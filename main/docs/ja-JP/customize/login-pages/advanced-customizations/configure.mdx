---
description: ACUL の設定方法を学ぶ
'og:image': https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
'og:title': ACUL を設定する
'og:url': https://auth0.com/docs/
permalink: configure
title: ACUL を設定する
'twitter:description': ACUL の設定方法を学ぶ
'twitter:title': ACUL を設定する
---

Auth0 Dashboard、Management API、Infrastructure-as-Code（IaC）ツールを使用して ACUL を設定できます。

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  ACUL を設定するには、カスタム設定ファイルとアセットがパブリック CDN 上でホストされていることを確認してください。
</Callout>

<div id="auth0-dashboard">
  ## Auth0 Dashboard
</div>

Auth0 Dashboard を使用して、ACUL で画面をカスタマイズできます。

A. [Dashboard &gt; Branding &gt; Universal Login](https://manage.auth0.com/dashboard/#/universal-login/customizations-new) に移動します。

1. **Customize authentication screens** を選択します。
   2. カスタマイズする <code>screen</code> を選択します。
   3. **Settings &gt; Rendering mode** から **Advanced mode** を選択します。
   4. （任意）**Use custom page template** を選択します。
   5. （任意）**Advanced mode filters** を選択します。

<Frame>
  <img src="/docs/images/cdy7uua7fh8z/ACUL/aculDashboard.png" alt="Auth0 Dashboard ACUL ページ" />
</Frame>

6. 独自の HTML `Head Tags` を追加するには、**Head Tags** を選択します。
   7. （任意）ブラウザーに送信する追加データを選択するには、**Additional Data** を選択します。

B. **Save &amp; Publish** を選択します。

**ACUL レンダリング構成オブジェクトを使用する**

ACUL レンダリング構成は、ACUL を有効化し、画面ごとに利用可能なすべてのプロパティを列挙するために使用される単一の JSON オブジェクトです。これらのプロパティは、特定のアセット、コンテキストデータ、および適用可能な条件付きロジックで構成されます。ACUL レンダリング構成オブジェクトは、IaC ツールおよび Auth0 Management API と併用できます。

詳細については、[`/v2/prompts/patch-rendering`](https://auth0.com/docs/api/management/v2/prompts/patch-rendering) エンドポイントを参照してください。

<div id="the-acul-rendering-configuration-object">
  ## ACUL レンダリング設定オブジェクト
</div>

ACUL レンダリング設定は 1 つの JSON オブジェクトであり、ACUL を有効にし、各画面ごとに利用可能なすべてのプロパティを列挙するために使用されます。これらのプロパティは、特定のアセット、コンテキストデータ、および適用可能な条件付きロジックで構成されます。ACUL レンダリング設定オブジェクトは、IaC ツールや Auth0 Management API と併用できます。

詳細については、[`/v2/prompts/patch-rendering`](https://auth0.com/docs/api/management/v2/prompts/patch-rendering) エンドポイントを参照してください。

<div id="infrastructure-as-code-iac">
  ## Infrastructure-as-Code (IaC)
</div>

IaC ツールを使用して ACUL を構成できます。使用できるツールには次のものがあります:

* **Auth0 CLI** の [`auth0 acul`](https://auth0.github.io/auth0-cli/auth0_acul.html) コマンド
  * **Auth0 Terraform Provider** の [`auth0_prompt_screen_renderer`](https://registry.terraform.io/providers/auth0/auth0/latest/docs/resources/prompt_screen_renderer) リソース
  * **Auth0 Deploy CLI** の [`prompts`](https://github.com/auth0/auth0-deploy-cli/blob/master/docs/resource-specific-documentation.md#prompts) リソース

<div id="auth0-management-api">
  ## Auth0 Management API
</div>

ACUL Management API エンドポイントを使用して、Universal Login 画面を個別に設定したり、1 回の API 呼び出しで複数の画面をまとめて更新したりできます。リクエストには [Management API アクセストークン](/docs/ja-JP/secure/tokens/access-tokens/management-api-access-tokens) が必要です。

**個別画面の設定**

* [`GET /v2/prompts/{prompt}/screen/{screen}/rendering` エンドポイント](/docs/ja-JP/api/management/v2/prompts/get-rendering)を使用すると、特定の画面の設定を取得できます。

  * [`PATCH /v2/prompts/{prompt}/screen/{screen}/rendering` エンドポイント](/docs/ja-JP/api/management/v2/prompts/patch-rendering)を使用すると、特定の画面で ACUL を設定できます。

**画面設定の一括更新**

* [`GET /v2/prompts/rendering` エンドポイント](/docs/ja-JP/api/management/v2/prompts/get-all-rendering)を使用すると、すべての画面の設定を取得できます。

  * [`PATCH /v2/prompts/rendering` エンドポイント](/docs/ja-JP/api/management/v2/prompts/patch-bulk-rendering)を使用すると、画面を一括で更新できます。