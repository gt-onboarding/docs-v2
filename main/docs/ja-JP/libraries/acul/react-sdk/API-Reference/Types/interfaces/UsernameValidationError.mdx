---
title: "UsernameValidationError"
---

バリデーション中に返される、単一のユーザー名検証エラーを表します。

各エラーには、機械可読の `code` と人間が読める `message` が含まれます。

```ts Example
export interface UsernameValidationError {
  /**
   * 失敗した検証ルールの一意の識別子。
   * 
   * 例: `"username-too-short"`, `"username-invalid-characters"`
   */
  code: string;

  /**
   * A human-readable description of the error.
   * 
   * Example: `"Username must be at least 3 characters long."`
   */
  message: string;

  /**
   * The field associated with the validation error, typically "username", "email", or "phone".
   */
  field: string;
}
```


<div id="properties">
  ## プロパティ
</div>

<ParamField body='code' type='string'>

失敗した検証ルールを一意に識別するための識別子。

例: `"username-too-short"`, `"username-invalid-characters"`

</ParamField>

<ParamField body='field' type='string'>

検証エラーが発生したフィールド。通常は "username"、"email"、または "phone" です。

</ParamField>

<ParamField body='message' type='string'>

エラー内容を人間が読める形式で記述したもの。

例: `"Username must be at least 3 characters long."`
</ParamField>