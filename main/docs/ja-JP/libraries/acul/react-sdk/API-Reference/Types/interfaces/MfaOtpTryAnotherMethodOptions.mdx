---
title: "MfaOtpTryAnotherMethodOptions"
---

別のMFA（多要素認証）手段を試すためのオプション。

```ts Example
export interface TryAnotherMethodOptions {
  /**
   * 任意の追加カスタムオプション。
   */
  [key: string]: string | number | boolean | undefined;
}
```


<div id="indexable">
  ## インデックス可能
</div>

\[`key`: `string`\]: `string` \| `number` \| `boolean` \| `undefined`

追加のカスタムオプションを指定できます。