---
title: "MfaVoiceChallengeContinueOptions"
---

音声チャレンジ用コードを送信する際に指定するオプション。

```ts Example
export interface MfaVoiceChallengeContinueOptions {
  /**
   * The verification code received via voice call.
   */
  code: string;

  /**
   * デバイスを30日間記憶し、今後のMFA（多要素認証）チャレンジをスキップするオプションフラグ。
   */
  rememberDevice?: boolean;

  /**
   * Additional custom options to pass with the request.
   */
  [key: string]: string | number | boolean | undefined;
}
```


<div id="indexable">
  ## インデックス可能
</div>

\[`key`: `string`\]: `string` \| `number` \| `boolean` \| `undefined`

リクエストに渡す追加のカスタムオプション。

<div id="properties">
  ## プロパティ
</div>

<ParamField body='code' type='string'>
音声通話で受信した検証コード。
</ParamField>

<ParamField body='rememberDevice?' type='boolean'>
デバイスを 30 日間記憶し、今後の MFA（多要素認証）チャレンジをスキップするための任意のフラグ。
</ParamField>