---
title: "MfaVoiceChallengeContinueOptions"
---

Options permettant de soumettre le code du défi vocal.

```ts Example
export interface MfaVoiceChallengeContinueOptions {
  /**
   * Le code de vérification reçu par appel vocal.
   */
  code: string;

  /**
   * Indicateur optionnel pour mémoriser l'appareil pendant 30 jours, en sautant les futurs défis AMF (MFA).
   */
  rememberDevice?: boolean;

  /**
   * Options personnalisées supplémentaires à transmettre avec la requête.
   */
  [key: string]: string | number | boolean | undefined;
}
```


<div id="indexable">
  ## Indexable
</div>

\[`key`: `string`\]: `string` \| `number` \| `boolean` \| `undefined`

Options personnalisées supplémentaires à inclure dans la requête.

<div id="properties">
  ## Propriétés
</div>

<ParamField body='code' type='string'>

Le code de vérification reçu par appel vocal.

</ParamField>

<ParamField body='rememberDevice?' type='boolean'>

Indicateur optionnel permettant de mémoriser l’appareil pendant 30&nbsp;jours et d’ignorer les futures demandes d’AMF (MFA).
</ParamField>