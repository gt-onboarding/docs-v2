---
title: "ScreenMembersOnMfaEnrollResult"
---

ScreenMembersOnMfaEnrollResult

```ts Example
export interface ScreenMembersOnMfaEnrollResult extends ScreenMembers {
  /**
   * Screen-specific data containing the status of the MFA enrollment.
   * @type {{ status: string; } | null}
   */
  data: {
    /**
     * L'état du processus d'inscription AMF (MFA).
     * Les valeurs possibles peuvent inclure « success », « failure » ou d'autres codes d'état spécifiques.
     * Cet état peut être utilisé pour afficher un message approprié à l'utilisateur.
     * @type {string}
     */
    status: string;
  } | null;
}
```


<div id="properties">
  ## Propriétés
</div>

<ParamField body='captcha' type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/CaptchaContext">CaptchaContext</a></span>}/>

<ParamField body='captchaImage' type='string'/>

<ParamField body='captchaProvider' type='string'/>

<ParamField body='captchaSiteKey' type='string'/>

<ParamField body='data' type='status'>

Données propres à l'écran.

<div id="type-declaration">
  #### Déclaration de type
</div>

\{ `status`: `string`; \}

<div id="status">
  #### status
</div>

L'état du processus d'inscription à l'authentification multifacteur (AMF (MFA)).
Les valeurs possibles incluent "success", "failure" ou d'autres codes d'état spécifiques.
Cet état peut être utilisé pour afficher un message approprié à l'utilisateur.

`null`

</ParamField>

<ParamField body='isCaptchaAvailable' type='boolean'/>

<ParamField body='links' type='Record'/>

<ParamField body='name' type='string'/>

<ParamField body='texts' type='Record'/>