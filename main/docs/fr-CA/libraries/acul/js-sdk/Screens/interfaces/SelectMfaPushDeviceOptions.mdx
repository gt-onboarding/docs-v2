---
title: "SelectMfaPushDeviceOptions"
---

Options pour sélectionner un appareil de notification push pour l’authentification multifacteur (AMF, MFA).

```ts Example
export interface SelectMfaPushDeviceOptions {
  /**
   * L'index de l'appareil à sélectionner dans la liste des appareils inscrits.
   * L'index est basé sur 0.
   */
  deviceIndex: number;
  [key: string]: string | number | boolean | undefined;
}
```


<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body='deviceIndex' type='number'>
L’index de l’appareil à sélectionner dans la liste des appareils inscrits.
L’index est à base 0.
</ParamField>