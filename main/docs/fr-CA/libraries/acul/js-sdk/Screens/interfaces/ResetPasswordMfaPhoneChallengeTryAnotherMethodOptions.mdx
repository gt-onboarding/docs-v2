---
title: "ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions"
---

TryAnotherMethodOptions

```ts Example
export interface TryAnotherMethodOptions extends CustomOptions {
  /**
   * La méthode de livraison qui a été initialement présentée ou sélectionnée sur l'écran actuel.
   * Ceci est requis par le point de terminaison de l'API pour traiter correctement l'action « pick-authenticator ».
   * - `sms` : Indique que l'écran actuel était destiné au défi SMS.
   * - `voice` : Indique que l'écran actuel était destiné au défi vocal.
   * @type {'sms' | 'voice'}
   */
  type: 'sms' | 'voice';
}
```


<div id="indexable">
  ## Indexable
</div>

\[`key`: `string`\]: `string` \| `number` \| `boolean` \| `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body='type' type='"sms" | "voice"'>
La méthode d'envoi qui a été initialement présentée ou sélectionnée sur l'écran en cours.
Elle est requise par le point de terminaison de l'API pour traiter correctement l'action `pick-authenticator`.
- `sms` : Indique que l'écran en cours était pour un défi SMS.
- `voice` : Indique que l'écran en cours était pour un défi vocal.
</ParamField>