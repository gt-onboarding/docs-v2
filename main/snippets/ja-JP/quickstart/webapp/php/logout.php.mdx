```php logout.php lines
<?php

  declare(strict_types=1);

  /**
   * アプリケーションセッションをクリアし、Auth0 ログアウトエンドポイントにリダイレクトします。
   *
   * その後、ユーザーはインデックスルートにリダイレクトされます。
   */

  header(sprintf('Location: %s', $sdk->logout()));
```
