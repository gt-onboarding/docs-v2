```js server.js highlight={2-16} lines 
const express = require('express');
const { auth, requiresAuth } = require('express-openid-connect');
const app = express();

const config = {
  authRequired: false,
  auth0Logout: true,
  baseURL: 'http://localhost:3000',
  clientID: '{yourClientId}',
  issuerBaseURL: 'https://{yourDomain}',
  secret: 'LONG_RANDOM_STRING'
};

// `auth` ルーターは /login、/logout、
// /callback ルートを baseURL にアタッチします
app.use(auth(config));

// req.oidc.isAuthenticated は auth ルーターから提供されます
app.get('/', (req, res) => {
  res.send(
    req.oidc.isAuthenticated() ? 'ログイン済み' : 'ログアウト済み'
  )
});

// /profile ルートはユーザープロファイルを JSON で表示します
app.get('/profile', requiresAuth(), (req, res) => {
  res.send(JSON.stringify(req.oidc.user, null, 2));
});

app.listen(3000, function() {
  console.log('Listening on http://localhost:3000');
});
```
