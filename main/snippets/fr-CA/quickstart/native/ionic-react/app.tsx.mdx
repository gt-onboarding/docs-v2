```tsx app.tsx lines 
// Importer les plugiciels app et browser de Capacitor, nous donnant accès à `addListener` et `appUrlOpen`,
// ainsi qu'aux composants nécessaires pour Auth0 et React
import { App as CapApp } from '@capacitor/app';
import { Browser } from '@capacitor/browser';
import { useEffect } from 'react';
import { useAuth0 } from '@auth0/auth0-react';

// ...

const App: React.FC = () => {
  // Obtenir le gestionnaire de rappel du hook React d'Auth0
  const { handleRedirectCallback } = useAuth0();

  useEffect(() => {
    // Gérer l'événement 'appUrlOpen' et appeler `handleRedirectCallback`
    CapApp.addListener('appUrlOpen', async ({ url }) => {
      if (url.includes('state') && (url.includes('code') || url.includes('error'))) {
        await handleRedirectCallback(url);
      }
      // Aucune opération sur Android
      await Browser.close();
    });
  }, [handleRedirectCallback]);

  // ..
};// Importer les plugiciels app et browser de Capacitor, nous donnant accès à `addListener` et `appUrlOpen`,
// ainsi qu'aux composants nécessaires pour Auth0 et React
import { App as CapApp } from '@capacitor/app';
import { Browser } from '@capacitor/browser';
import { useEffect } from 'react';
import { useAuth0 } from '@auth0/auth0-react';

// ...

const App: React.FC = () => {
  // Obtenir le gestionnaire de rappel du hook React d'Auth0
  const { handleRedirectCallback } = useAuth0();

  useEffect(() => {
    // Gérer l'événement 'appUrlOpen' et appeler `handleRedirectCallback`
    CapApp.addListener('appUrlOpen', async ({ url }) => {
      if (url.includes('state') && (url.includes('code') || url.includes('error'))) {
        await handleRedirectCallback(url);
      }
      // Aucune opération sur Android
      await Browser.close();
    });
  }, [handleRedirectCallback]);

  // ..
};
```
