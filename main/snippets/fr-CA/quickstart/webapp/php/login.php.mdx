```php login.php lines
<?php

  declare(strict_types=1);

  /**
   * Prépare la session de l'application et redirige vers la page Universal Login d'Auth0.
   *
   * L'utilisateur sera redirigé vers votre route de rappel pour terminer le flux d'authentification.
   */

  header(sprintf('Location: %s', $sdk->login()));
```
